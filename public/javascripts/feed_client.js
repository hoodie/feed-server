// Generated by CoffeeScript 1.6.2
var FEED_MODEL, socket,
  _this = this;

console.info('hello');

FEED_MODEL = {};

socket = io.connect('http://localhost:3000');

socket.on('welcome', function(data) {
  console.log(data);
  return socket.emit('init_model', "soon to be individual nickname");
});

socket.on('new_feed_item', function(item) {
  var _base, _name, _name1, _ref, _ref1;

  if ((_ref = FEED_MODEL[_name = item.feedid]) == null) {
    FEED_MODEL[_name] = {};
  }
  if ((_ref1 = (_base = FEED_MODEL[item.feedid])[_name1 = item.id]) == null) {
    _base[_name1] = item;
  }
  console.log(Object.keys(FEED_MODEL).length, 'feeds');
  return console.log("" + item.feed_title + (Object.keys(FEED_MODEL[item.feedid]).length) + " items");
});
